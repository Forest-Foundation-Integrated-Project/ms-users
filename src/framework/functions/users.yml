createUser:
  handler: ${self:custom.funcDir}/createUser.handler
  events:
    - http:
        path: ${self:custom.baseApi}/user
        method: post
        cors:
          origin: '*'
          headers: '*'